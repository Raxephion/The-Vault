# Required to interact with Hugging Face Hub (uploading, repo management)
huggingface_hub>=0.21.0

# Needed for Git operations (used under the hood by huggingface_hub)
gitpython>=3.1.0

# Optional: If using CLI features like huggingface-cli login
huggingface_hub[cli]
